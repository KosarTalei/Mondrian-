 Draw a Mondrian-esque square with red, blue, white, or yellow blocks inside the main square.
 I like how a different Mondrian appears each time the program is called and how different
 each format looks even though the code is basically the same.


# Output
The screenshot from the output of program is in the docs directory.


# JavaFx 11

<ul>
<li>
clone in your computer: 

```
git clone http://apj.ce.kntu.ac.ir/git/root/<YOUR_REPO_URL>
cd YOUR_REPO_URL
```


</li>
<li>
    <p>Run Javafx GUI</p>
    
```
mvn clean javafx:run
```

</li>

<li>
    <p>Run tests</p>
    
```
mvn clean test
```
</li>

<li>
<p> push your change to the repository: </p>
 
```
git add .
git commit -m "commit message"
git push
```
</li>
</ul>
